<div>
  <% if notice.present? %>
    <p><%= notice %></p>
  <% end %>

  <div>
    <h1 class="font-bold text-4xl mb-4">Users</h1>
      <%= link_to 'New user', new_user_registration_path, class: "rounded-lg py-3 px-5 bg-sky-500 hover:bg-sky-700 inline-block font-medium" %>
  </div>

  <%= link_to 'Main page', root_path, class: "rounded-lg py-3 px-5 bg-sky-500 hover:bg-sky-700 inline-block font-medium mt-2" %>

  <div class="mb-2 mt-2">
    <%= render @users %>
  </div>
</div>
